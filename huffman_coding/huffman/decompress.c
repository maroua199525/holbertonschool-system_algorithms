#include "../huffman.h"

/**
 * decompress - decompress a file using Huffman codes
 *
 * @in_file: input file (compressed)
 * @out_file: output file (decompressed)
 *
 * Return: EXIT_SUCCESS on success, EXIT_FAILURE on failure
 */
int decompress(const char *in_file, const char *out_file)
{
	int ret = EXIT_SUCCESS;

	/* Get huffman codes */

	/* Create decompressed file */

	return (ret);
}